FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit==1.41.1 requests==2.32.3

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0", "--server.headless=true"]
